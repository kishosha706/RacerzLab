{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default RaceLab Garage desktop window with dialog permissions for native file picker.",
  "windows": ["main"],
  "permissions": [
    "dialog:default"
  ]
}
