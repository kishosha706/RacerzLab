# Backend/Frontend Contract Reference

The canonical current integration contract is
[`docs/contracts/backend_frontend_contract.md`](contracts/backend_frontend_contract.md).

This pointer intentionally contains no duplicate schema or enum list. Removed
compatibility fields and endpoints must not be restored from historical notes.
